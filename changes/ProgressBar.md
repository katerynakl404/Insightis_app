# ProgressBar — prod → expected

| Part | Current (prod) | Expected |
|---|---|---|
| Track | `background` | `Surface/Chips` (sub-perceptible shift) |
| Fill | `primary-gradient` | `Brand/Primary` / gradient (value re-bound, ≈ same) |

> Essentially **no visible change** — fill and track stay teal. Height 4px, radius `lg`.

## No change (—)
Height, radius, gradient direction.
