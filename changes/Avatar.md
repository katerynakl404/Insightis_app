# Avatar — prod → expected

| Part | Current (prod) | Expected |
|---|---|---|
| Initials fallback bg | `background` (light surface) | `Brand/Primary` (filled teal) |
| Counter chip bg | `chip` `#FAFAFA` | `Surface/Chips` `#F8FAFC` |
| Text | `content-secondary` | `Text/Secondary` |

> Notable: prod fallback uses a light neutral surface; the kit's expected fills it with `Brand/Primary`. Confirm desired direction with design.

## No change (—)
Size 32px, radius `full`.
