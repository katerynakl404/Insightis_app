node_modules/
dist/
.turbo/
.DS_Store
*.log
.env

# Local IDE / Claude Code session files
.claude/

# Build-prompt task files (not part of the handoff artifact)
claude-code/task-*.md

# Local release notes (kept local, never pushed)
release-notes/

# Accessibility self-check files (local audit notes, not part of the handoff artifact)
a11y/

# Archived design decisions (rejected iterations preserved locally for recovery).
# Never referenced by live pages or kit components — see archive/README.md.
archive/

# Claude Design (claude.ai/design) sync tooling — local-only, NEVER published with the handoff
# artifact. All of it lives under one folder now: converter scripts (.ds-sync/), build output
# (ds-bundle/), machine state (.design-sync/), the ds-react wrapper package, and the sync prompt.
# See .claude-design/README.md. The driver MUST be run with cwd = .claude-design/.
.claude-design/
