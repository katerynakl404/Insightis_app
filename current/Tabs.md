# Tabs — current (prod, 04.06)

Trigger: text-sm, `disabled:opacity-50`. Active: border-bottom 2px `accent` + text `accent`;
inactive text `content-secondary`. Hover bg `State/Hover`.
04.06: trigger icons added — `[&_svg]:size-5 [&_svg]:text-content-light`, active svg `text-secondary`.
