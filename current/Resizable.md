# Resizable — current (prod, 04.06)

Split panels with a drag handle. Group `flex h-full w-full`.
Handle: `relative flex w-px items-center justify-center bg-border`;
hover `[data-resize-handle-state=hover]:bg-primary/30`; drag `[data-resize-handle-state=drag]:bg-primary/50`.
Tokens unchanged from the rest of the system.
