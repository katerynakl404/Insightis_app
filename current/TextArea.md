# TextArea — current (prod, 04.06)

Reworked in 04.06 to a cva-based component: `block w-full min-w-0 resize-none`, `outline-none`,
border `border`, radius `md`, text `content-body` text-sm, placeholder `content-secondary`,
focus border `accent`, disabled `opacity-50`. Auto-grows up to a max height.
*(Was previously `rounded-3xl` + `bg-background` + `px-4 py-4`.)*
