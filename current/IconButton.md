# IconButton — current (prod)

Shares the Button cva. Default variant = primary → `border-gradient-inner-accent` (accent fill),
gradient hover/press, icon = background colour. Default size md (36×36), radius `md`, icon 18px.
Outline/secondary variants available (border `border`/`secondary`, hover `accent`).
