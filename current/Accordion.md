# Accordion — current (prod, 04.06)

Live on prod since the 04.06 drop. Item header `flex justify-between items-center gap-3`,
`py-3`, text-sm `font-medium` `content-primary`; divider `not-last:border-b` (`border`);
container radius `lg`; open/close uses `animate-accordion-down` / `accordion-up` (0.2s).
Chevron rotates on open. Tokens unchanged from the rest of the system.
