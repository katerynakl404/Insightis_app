# Stepper — current (prod, 04.06)

**Headless** component. Renders the current step via a render function and exposes the
stepper API (currentStep, totalSteps, next/prev…) via ref for imperative control.
No built-in visual tokens — the consumer styles each step. `use-stepper` holds the logic.
