# Dropdown menu — current (prod)

Content: `z-50 rounded-md border p-1 border-border bg-background text-content-primary shadow-md`.
Item: `flex items-center rounded py-0.5 px-1.5/pl-8 text-sm`; hover/focus `focus:bg-primary/10`
(some item types `focus:bg-border`). Label `px-2 py-1.5 font-semibold text-sm`. Submenu `rounded-md border bg-background shadow-lg`.
