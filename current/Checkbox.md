# Checkbox — current (prod)

`size-5` (20×20), radius `md`, `border border-border`, `bg-card`.
- Checked: `bg-accent`, `text-white`, `border-transparent`, mark `[&>svg]:size-3.5`.
- Disabled: `opacity-50`.
- Focus: `ring-2 ring-ring ring-offset-2`.
- Label: `font-medium text-sm`, gaps 1.5/2/3.
