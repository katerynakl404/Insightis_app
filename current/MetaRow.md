# MetaRow — current (prod)

⚠ **Pending capture.** Whether the live `/chats` page on prod (<https://insightis-app.devart.info/chats>) shows a count / Select-all row above the list is not verified — see the same note on `ChatRow.md`. The Expected MetaRow spec stands, but the "Current (prod) — did not exist" cell in the kit and in `../changes/MetaRow.md` is a placeholder, not a confirmed fact, until the prod UI is captured.
