# Avatar — current (prod)

Sizes `size-5/7/8/9/10/11` (default md = `size-8` / 32px), radius `full` (default).
- Image: `aspect-square h-full w-full`.
- Fallback: `flex items-center justify-center bg-background` (light neutral surface).
- Counter / initials styled by consumer on top of the fallback surface.
