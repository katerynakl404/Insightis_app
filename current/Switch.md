# Switch — current (prod, 04.06)

Track `h-5 w-9` (36×20), thumb `size-4`, radius `full`, `border`.
- Checked: `bg-accent` + `border-accent`, thumb `bg-card`. *(04.06: changed from `primary` → `accent`.)*
- Unchecked: `bg-background` + `border-border`, thumb `bg-content-light`.
- Disabled: `opacity-50`.
- Focus: `ring-2 ring-ring ring-offset-2`.
