# Spinner — current (prod)

`animate-spin`, sizes `size-3/4/5/6/8` (default sm = `size-4`). Colour variants: primary (default),
content-secondary, content-light, accent, success, red, orange, white, inherit. Default `text-primary`.
