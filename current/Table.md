# Table — current (prod)

Wrapper `relative w-full overflow-x-auto rounded-md border border-border bg-card`.
Table `w-full caption-bottom text-sm`. Header text-xs `content-secondary`, body `content-body`,
row border-bottom `border` (`Stroke/Border`), row hover `State/Hover`, cell padding 12/16px.
