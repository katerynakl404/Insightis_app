# ProgressBar — current (prod)

Track `relative w-full overflow-hidden rounded-full bg-background`, height per size (default ~`h-1`/`h-2`),
radius `lg`. Indicator `bg-primary-gradient` (primary→secondary) or `bg-secondary`, `transition-all`.
