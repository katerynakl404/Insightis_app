# Typography — current (prod)

DM Sans, standard Tailwind scale. Semantic variants (Typography component):
H1 `text-3xl md:text-4xl` medium · H2 `text-2xl md:text-3xl` · H3 `md:text-2xl` · H4 `text-lg md:text-xl`
· H5 `text-base md:text-lg` · H6 `text-sm md:text-base` · p `text-sm` normal · span `text-sm` medium ·
lead `text-xl` semibold · large `text-lg` semibold · code mono semibold · blockquote italic.
Weights 300–900. 04.06: Typography made polymorphic (`as` element) — no visual change.
