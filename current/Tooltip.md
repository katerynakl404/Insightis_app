# Tooltip — current (prod)

Content: `z-50 px-3 py-1.5 rounded-md bg-chip text-content-primary text-sm`, fade/zoom + slide-in
per side, transform-origin from Radix. Built on `@radix-ui/react-tooltip`.
